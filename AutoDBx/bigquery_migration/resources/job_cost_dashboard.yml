# resources/job_cost_dashboard.yml
# Simplified approach - create just the dashboard with embedded queries

resources:
  dashboards:
    job_cost_analysis_dashboard:
      display_name: "Job Cost Analysis"
      warehouse_id: ${var.warehouse_id}
      parent_path: "/Shared/AutoDbx/Dashboards"
      file_path: "../src/dashboard/dashboard_definition.json"